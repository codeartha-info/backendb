server:
  port: 8181

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codeartha
    username: root
    password: Root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true